[package]
name = "rsyncp"
version = "0.1.0"
edition = "2024"

[dependencies]
unicode-width = "0.2.2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
debug = false
overflow-checks = true
